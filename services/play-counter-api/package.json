{
  "name": "book-song-play-counter-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check api/plays.js && node --test tests/*.test.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
